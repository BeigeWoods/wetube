extends ../base
include ../mixins/video

block content
  div.profile__data
    if loggedInUser.socialOnly
      img(src=user.avatarUrl).avatar
    else 
      if user.avatarUrl
        img(src="/" + user.avatarUrl).avatar
      else
        span.avatar 🎅
    h3=user.name
    div.deleteAccount
      a(href=`/users/${loggedInUser._id}/leaveUser`) Delete Account
  div.video-grid
    each video in user.videos
      +video(video)
    else
      span.empty__message No videos found